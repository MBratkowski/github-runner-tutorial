name: Claude PR Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

permissions:
  contents: read
  pull-requests: write
  id-token: write

concurrency:
  group: claude-pr-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  review:
    if: ${{ !github.event.pull_request.draft }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          include_fix_links: true
          claude_args: '--model claude-sonnet-4-6 --max-turns 15 --allowedTools "Bash(gh:*)"'
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            You are an expert Android/Kotlin code reviewer. Review this PR thoroughly.

            ## Steps
            1. Run: `gh pr diff ${{ github.event.pull_request.number }}`
            2. Read `CLAUDE.md` for project conventions and review guidelines
            3. Analyze the diff for issues

            ## Review Focus (Android-specific)
            - Coroutine usage: proper scope (viewModelScope), structured concurrency, suspend functions
            - Compose recomposition: stable parameters, proper use of remember/derivedStateOf, state hoisting
            - Room database: Flow for reactive queries, proper @Transaction usage, migration strategy
            - Memory leaks: context references, coroutine scope lifecycle, callback cleanup
            - Null safety and error handling
            - General: bugs, security issues, performance, code style

            ## Severity Ratings
            Use these severity levels for each finding:
            - **CRITICAL** — bugs, crashes, security vulnerabilities, data loss risks
            - **WARNING** — performance issues, potential memory leaks, incorrect patterns
            - **SUGGESTION** — better approaches, cleaner patterns, maintainability improvements
            - **NITPICK** — style, naming, minor formatting

            ## Output Format — Inline Comments
            Post your review using the GitHub API with inline comments pinned to specific lines.

            ### How to determine line numbers
            1. Run `gh pr diff ${{ github.event.pull_request.number }}`
            2. In the diff output, look at the `@@` hunk headers, e.g. `@@ -10,6 +12,8 @@`
               - The `+12,8` means new file starts at line 12 for 8 lines
            3. Count lines with `+` prefix (added) or ` ` prefix (context) from the hunk start
            4. The `line` value in your JSON must be the LINE NUMBER IN THE NEW VERSION OF THE FILE
            5. You can also read the full file to verify line numbers

            ### CRITICAL RULES for the JSON
            - Use `"line"` (integer) — the line number in the file, NOT the diff position
            - Use `"side": "RIGHT"` — always comment on the new version
            - Do NOT include a `"position"` field — it will override `line` and break placement
            - Only comment on lines that appear in the diff (added or modified lines)

            ### Example
            If the diff shows:
            ```
            @@ -5,6 +5,10 @@ class Foo {
            +    fun badMethod() {
            +        GlobalScope.launch { }  // line 7 in new file
            +    }
            ```
            Then use `"line": 7` to comment on the GlobalScope line.

            ### Build and post the review
            Create `/tmp/review.json`:
            ```json
            {
              "event": "COMMENT",
              "body": "## PR Review Summary\n\n<1-line overall assessment>",
              "comments": [
                {
                  "path": "app/src/main/java/com/example/todoapp/ui/todo/TodoViewModel.kt",
                  "line": 59,
                  "side": "RIGHT",
                  "body": "**[CRITICAL]** `GlobalScope.launch` leaks — use `viewModelScope`."
                }
              ]
            }
            ```

            Post with:
            ```bash
            gh api repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/reviews \
              --method POST \
              --input /tmp/review.json
            ```

            Rules:
            - Each finding = one inline comment on the exact line
            - Use severity prefix: **[CRITICAL]**, **[WARNING]**, **[SUGGESTION]**, **[NITPICK]**
            - The `body` field is a short summary of the overall review
            - Only flag real issues, not stylistic preferences unless they violate CLAUDE.md conventions
            - Be concise in each comment
